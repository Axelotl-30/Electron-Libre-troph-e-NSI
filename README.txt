pour l'application:
aucune installation de python ou de module n'est requise pour l'application
garder le dossier internal avec le .exe pour garantir le fonctionnement 
le contenu du dossier du jeu peut être déplacé n'importe où dans l'explorateur de fichier 

pour le code source:
python 3.11 

modules:  windows / linux   ! python doit être dans le PATH pour utiliser pip!
  pyxel: pip install -U pyxel / sudo pip3 install -U pyxel
  numpy: pip install numpy / sudo pip3 install numpy
  
installer libsdl2-dev pour installer des modules avec linux

pour lancer l'application python il faut executer le fichier Electron Libre.py
